# Example env for docker compose (Postgres + API).
# Copy to .env or pass via docker compose --env-file.
# DATABASE_URL is set in docker-compose.yml for the api service; override here if needed.

DATABASE_URL=postgresql+psycopg2://aml:aml@postgres:5432/aml
# AML_API_KEYS set in docker-compose.yml (e.g. dev:dev_key) for mutation auth.
# AML_ALLOW_SCHEMA_UPGRADE not required when using Postgres (schema via Alembic).
