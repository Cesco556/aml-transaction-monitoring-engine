# AML Monitoring - Environment overrides
# Copy to .env and set values. Never commit .env with real secrets.

# Config file path (optional; defaults to config/default.yaml)
# AML_CONFIG_PATH=config/dev.yaml

# Database URL (use AML_DATABASE_URL or DATABASE_URL; Postgres: postgresql+psycopg2://user:pass@host:5432/dbname)
# AML_DATABASE_URL=sqlite:///./data/aml.db

# Log level: DEBUG, INFO, WARNING, ERROR
# AML_LOG_LEVEL=INFO

# API (when using serve-api)
# AML_API_HOST=0.0.0.0
# AML_API_PORT=8000

# Allow SQLite schema auto-upgrade (local dev only)
# AML_ALLOW_SCHEMA_UPGRADE=true

# Actor for CLI audit log traceability (default: cli)
# AML_ACTOR=analyst

# API keys for mutating endpoints (PATCH/POST). Format: "name1:key1,name2:key2"
# Example: AML_API_KEYS=admin:dev_admin_key,analyst1:dev_analyst_key
# If empty/unset, defaults to dev:dev_key (dev-only; do not use in production).
# AML_API_KEYS=dev:dev_key
